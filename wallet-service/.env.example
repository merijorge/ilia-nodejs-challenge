# Database Configuration
# For local development with Docker: postgresql://postgres:password@postgres-wallet:5432/wallet
# For local development without Docker: postgresql://postgres:password@localhost:5432/wallet
DATABASE_URL="postgresql://postgres:password@localhost:5432/wallet?schema=public"

# JWT Configuration
# External JWT for user-facing endpoints (must match User Service)
JWT_PRIVATE_KEY="ILIACHALLENGE"

# Internal JWT for service-to-service communication (must match User Service)
JWT_INTERNAL_KEY="ILIACHALLENGE_INTERNAL"

# Server Configuration
PORT=3001
NODE_ENV=development

# Other Services
USER_SERVICE_URL="http://localhost:3002"

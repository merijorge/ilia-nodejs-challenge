# Database Configuration
# For local development with Docker: postgresql://postgres:password@postgres-user:5432/users
# For local development without Docker: postgresql://postgres:password@localhost:5432/users
DATABASE_URL="postgresql://postgres:password@localhost:5433/users?schema=public"

# JWT Configuration
# External JWT for user-facing endpoints (must match Wallet Service)
JWT_PRIVATE_KEY="ILIACHALLENGE"

# Internal JWT for service-to-service communication (must match Wallet Service)
JWT_INTERNAL_KEY="ILIACHALLENGE_INTERNAL"

# Wallet Service Configuration
# For local development with Docker: http://wallet-service:3001
# For local development without Docker: http://localhost:3001
WALLET_SERVICE_URL="http://wallet-service:3001"

# Server Configuration
PORT=3002
NODE_ENV=development
